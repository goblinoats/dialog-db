Always run these commands before considering work complete:

1. `cargo fmt --all` — format all code
2. `cargo clippy --all --all-targets --all-features -- -D warnings` — lint with all warnings as errors

Fix any issues found. Do not consider work complete until both pass cleanly.
